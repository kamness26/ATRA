"""
Sheet Service
Handles writing prompt and image data back to Google Sheets.
"""

def update_sheet(prompt: str, image_url: str) -> None:
    # Placeholder — will later use Google Sheets API
    print(f"📒 Updating sheet with prompt='{prompt[:40]}...', image='{image_url}'")

